---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03

must_haves:
  truths:
    - "All 11 CVEs from `uv audit` are resolved — `uv audit` exits 0 with no output"
    - "uv.lock reflects upgraded packages (aiohttp 3.13.5+, starlette 1.0.0+, fastapi 0.135.3+, etc.)"
    - "httpx is NOT listed under [project] dependencies — only in [dependency-groups] dev"
    - "All 201 existing tests remain green after dependency upgrades"
  artifacts:
    - path: "pyproject.toml"
      provides: "httpx moved to dev group; no unused production deps"
    - path: "uv.lock"
      provides: "Upgraded lockfile with CVE-fixed versions of aiohttp and pygments"
  key_links:
    - from: "pyproject.toml [project] dependencies"
      to: "uv.lock"
      via: "`uv lock --upgrade` resolves constraints"
      pattern: "aiohttp.*3\\.13\\.[5-9]"
---

<objective>
Upgrade all dependencies to current compatible versions, remediate all known CVEs, and move httpx from production dependencies to dev dependencies.

Purpose: Eliminates 11 known security vulnerabilities (all in aiohttp 3.13.3 and pygments 2.19.2), keeps the lockfile current, and removes a test-only library from the production Docker image.

Output: Refreshed uv.lock with all CVEs resolved; httpx moved to dev deps; production dep list clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade all dependencies and validate test suite</name>
  <read_first>
    - pyproject.toml — current dependency constraints before upgrade
    - uv.lock — current locked versions (to compare after upgrade)
  </read_first>
  <files>uv.lock, pyproject.toml</files>
  <action>
Upgrade all packages to their current compatible versions:

```bash
uv lock --upgrade
uv sync
```

Expected upgrades (verified 2026-04-13 research):
| Package | From | To | Risk |
|---------|------|----|------|
| aiohttp | 3.13.3 | 3.13.5 | LOW — fixes 10 CVEs |
| starlette | 0.52.1 | 1.0.0 | MEDIUM — major jump |
| fastapi | 0.135.1 | 0.135.3 | LOW |
| uvicorn | 0.42.0 | 0.44.0 | LOW |
| pytest | 9.0.2 | 9.0.3 | LOW |
| lxml | 6.0.2 | 6.0.4 | LOW |
| pygments | 2.19.2 | 2.20.0 | LOW — fixes 1 CVE |
| python-multipart | 0.0.22 | 0.0.26 | LOW |
| anyio | 4.12.1 | 4.13.0 | LOW |
| attrs | 25.4.0 | 26.1.0 | LOW |
| click | 8.3.1 | 8.3.2 | LOW |

Immediately run the full test suite — this is the gate per D-11:
```bash
uv run pytest tests/ -v
```

All 201 tests must remain green. Pay special attention to the starlette 0.52.1 → 1.0.0 major version jump.

FALLBACK (only if tests fail after starlette upgrade):
If any tests fail and the failure is traceable to starlette 1.0.0, pin the version:
1. Add `starlette<1.0.0` as a constraint in pyproject.toml under `[project] dependencies`
2. Re-run `uv lock --upgrade` to re-resolve with the pin
3. Run `uv sync && uv run pytest tests/ -v` to confirm tests pass
4. Document the pin and reason in the commit message

Do NOT commit the upgraded lock if any tests fail.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q --no-header` exits 0 with 201 tests passing
    - `grep 'aiohttp' uv.lock` shows version 3.13.5 or higher (CVE fix)
    - `grep 'pygments' uv.lock` shows version 2.20.0 or higher (CVE fix)
    - `uv run ruff check src/` still exits 0 (no regressions from upgraded packages)
  </acceptance_criteria>
  <done>uv.lock upgraded, all 201 tests green, aiohttp and pygments CVEs addressed by version upgrade.</done>
</task>

<task type="auto">
  <name>Task 2: Security audit and move httpx to dev dependencies</name>
  <read_first>
    - pyproject.toml — locate httpx in [project] dependencies and [dependency-groups] dev sections
  </read_first>
  <files>pyproject.toml, uv.lock</files>
  <action>
STEP 1 — Run security audit to confirm CVEs are resolved:
```bash
uv audit
```

Expected result: zero vulnerabilities reported (all 11 CVEs fixed by the Task 1 upgrade).

If `uv audit` still reports CVEs after the upgrade:
- CVEs with available fix: update the specific package to the fixed version
- CVEs without available fix: create `SECURITY.md` at the project root with this format for each unresolvable CVE:
  ```markdown
  # Security Notes

  ## Known CVEs Without Available Fix

  | CVE | Package | Status | Mitigation |
  |-----|---------|--------|------------|
  | CVE-XXXX-XXXXX | package-name vX.Y.Z | No fix available as of 2026-04-13 | [mitigation notes] |
  ```

STEP 2 — Move httpx from production deps to dev deps:

httpx is currently listed under `[project] dependencies` but is only used in `tests/test_api.py` (FastAPI TestClient). It must NOT be in production deps (it ends up in the Docker image unnecessarily) — move it to dev.

Run:
```bash
uv remove httpx
uv add --dev "httpx>=0.28.1"
```

STEP 3 — Verify tests still pass (httpx must still be available for test_api.py):
```bash
uv run pytest tests/ -q --no-header
```

All 201 tests must remain green. If httpx is not found in tests after the move, run `uv sync` to ensure the dev deps are installed.
  </action>
  <verify>
    <automated>uv audit && uv run pytest tests/ -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv audit` exits 0 with no vulnerabilities reported (or SECURITY.md created per D-15 for unresolvable CVEs)
    - `grep 'httpx' pyproject.toml` does NOT show httpx under `[project] dependencies` (production deps)
    - `grep 'httpx' pyproject.toml` DOES show httpx under `[dependency-groups] dev`
    - `uv run pytest tests/ -q --no-header` exits 0 with 201 tests passing (httpx still importable in tests)
  </acceptance_criteria>
  <done>Security audit clean. httpx moved to dev deps. DEP-01, DEP-02, DEP-03 satisfied.</done>
</task>

</tasks>

<verification>
Phase 1 Plan 02 complete when:
- `uv audit` exits clean (zero vulnerabilities)
- httpx absent from [project] dependencies, present in [dependency-groups] dev
- 201 tests green after all upgrades
- uv.lock contains aiohttp 3.13.5+ and pygments 2.20.0+
</verification>

<success_criteria>
1. `uv audit` reports zero vulnerabilities — all 11 CVEs resolved via upgrade (DEP-02)
2. uv.lock upgraded to current compatible versions for all 11 packages (DEP-01)
3. httpx moved from [project] dependencies to [dependency-groups] dev — not in Docker image (DEP-03)
4. All 201 existing tests remain green after all dependency changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
