---
phase: 04-testing-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_mqtt_output.py
autonomous: true
requirements: [TEST-02]
user_setup: []

must_haves:
  truths:
    - "MQTT adapter silently drops events when _client is None (not connected)"
    - "handle() returns without raising when broker is offline"
    - "handle_line_state_change() returns without raising when broker is offline"
  artifacts:
    - path: "tests/test_mqtt_output.py"
      provides: "TestMqttPublishOffline class with 3+ offline scenario tests"
      contains: "test_publish_while_offline, test_handle_line_state_offline, test_retry_on_reconnect"
  key_links:
    - from: "tests/test_mqtt_output.py"
      to: "src/adapters/mqtt.py"
      via: "MqttAdapter.handle() with _client=None"
      pattern: "if self\\._client is None"
    - from: "tests/test_mqtt_output.py"
      to: "src/adapters/mqtt.py"
      via: "MqttAdapter.handle_line_state_change() with _client=None"
      pattern: "async def handle_line_state_change"
---

<objective>
Test MQTT adapter publish-while-offline scenarios: verify graceful handling when broker is unavailable.

This plan addresses TEST-02 requirement. Focuses on offline behavior (drop without crash), not reconnect logic (covered by existing TestMqttReconnectLogging).

Purpose: Verify that MQTT adapter doesn't crash or deadlock when broker connection drops
Output: 3+ new async tests validating offline behavior
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/04-testing-validation/04-CONTEXT.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md §Tests (TEST-02 acceptance criteria)
@.planning/PROJECT.md

# Existing MQTT test patterns
@tests/test_mqtt_output.py (TestHandleEventTopics, TestMqttReconnectLogging classes)
  - _inject_client() helper pattern
  - _make_adapter(), _make_event(), _make_line_state() builders

# Production code under test
@src/adapters/mqtt.py (handle(), handle_line_state_change() methods)
  - Lines ~155-160: `if self._client is None: return`
  - Lines ~182-190: Similar check in handle_line_state_change()

From src/adapters/mqtt.py:
```python
async def handle(self, event: CallEvent, result: Optional[ResolveResult], *, line_state: Optional[LineState] = None) -> None:
    """Publish event to MQTT topics."""
    if not self._running or self._client is None:
        return  # Broker not connected — drop silently
    # ... publish logic

async def handle_line_state_change(self, line_state: LineState) -> None:
    """Publish line state change to MQTT topic."""
    if not self._running or self._client is None:
        return  # Broker not connected — drop silently
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestMqttPublishOffline class with offline event drop tests</name>
  <files>tests/test_mqtt_output.py</files>
  <action>
Add new test class `TestMqttPublishOffline` at the end of test_mqtt_output.py.

**Test: test_handle_returns_without_error_when_client_none**
- Setup: `adapter = _make_adapter()`, ensure `adapter._client is None` (default state)
- Action: `await adapter.handle(_make_event(), None)`
- Verify: No exception raised, function returns cleanly

**Test: test_handle_logs_nothing_when_client_offline**
- Setup: Create adapter with `_client = None`, set up caplog
- Action: `await adapter.handle(_make_event(), None)` with `caplog.at_level(logging.DEBUG)`
- Verify: No log records generated (offline is silent, not an error condition)

**Test: test_handle_line_state_change_returns_when_client_none**
- Setup: `adapter = _make_adapter()`, `adapter._client = None`
- Action: `await adapter.handle_line_state_change(_make_line_state())`
- Verify: No exception raised

**Test: test_handle_with_result_drops_silently**
- Setup: Create adapter with `_client = None`
- Action: `await adapter.handle(_make_event(), ResolveResult(name="Alice", number="+491234567890", source="test"))`
- Verify: No exception, event with resolved data is dropped without publishing

**Scenario: Multiple rapid calls while offline**
- Setup: Create adapter with `_client = None`
- Action: Call `await asyncio.gather(adapter.handle(...) for _ in range(5))`
- Verify: All calls complete without exception, no deadlock

Helper: Use `_make_event()`, `_make_line_state()`, `_make_adapter()` from existing file (already present).

Note: These tests verify the `if self._client is None: return` guard clause in handle() and handle_line_state_change(). They do NOT test reconnection logic — that's covered by TestMqttReconnectLogging.
  </action>
  <verify>
    <automated>pytest tests/test_mqtt_output.py::TestMqttPublishOffline -xvs</automated>
  </verify>
  <done>TestMqttPublishOffline class with 5 tests covering offline event drop scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Add TestMqttPublishWhileOfflineWithRetry (connection drop recovery)</name>
  <files>tests/test_mqtt_output.py</files>
  <action>
Add to TestMqttPublishOffline class:

**Scenario: Event dropped when offline, published after reconnect**
- Setup: 
  - Create adapter with `_client = None` (offline)
  - Create a call event
- Step 1: `await adapter.handle(event, None)` with `_client = None` — should be silent drop
- Step 2: Inject a real client via `_inject_client(adapter)` (simulating reconnect)
- Step 3: `await adapter.handle(event, None)` again with client present
- Verify: 
  - First handle() produces no published messages (offline)
  - Second handle() produces published messages (connected)
  - Different behavior confirms the `_client is None` guard is working

This test demonstrates that events are NOT queued during offline periods — they are discarded. When the adapter reconnects, only NEW events are published.

Use `_inject_client()` helper which was already defined in the file (returns published list).
  </action>
  <verify>
    <automated>pytest tests/test_mqtt_output.py::TestMqttPublishOffline::test_event_dropped_offline_published_online -xvs</automated>
  </verify>
  <done>Test added verifying offline drops; online publishes after reconnect</done>
</task>

<task type="auto">
  <name>Task 3: Verify no regressions in MQTT tests</name>
  <files>tests/test_mqtt_output.py</files>
  <action>
Run: `uv run pytest tests/test_mqtt_output.py -v --tb=short`

Verify:
- All existing MQTT tests still pass (TestSlugify, TestHandleEventTopics, TestMsnField, etc.)
- All new TestMqttPublishOffline tests pass
- No breaking changes to _make_adapter(), _inject_client(), _make_event(), _make_line_state() helpers
- Total test count increased by ~6 tests
  </action>
  <verify>
    <automated>uv run pytest tests/test_mqtt_output.py -v 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | wc -l</automated>
  </verify>
  <done>All MQTT tests pass (existing + new); TEST-02 covered</done>
</task>

<task type="auto">
  <name>Task 4: Run full test suite</name>
  <files>tests/test_mqtt_output.py</files>
  <action>
Run: `uv run pytest tests/ -v --tb=short`

Verify:
- All 201+ existing tests still pass
- New ~6 offline scenario tests pass
- No regressions in any other test file
  </action>
  <verify>
    <automated>uv run pytest tests/ -v 2>&1 | tail -5</automated>
  </verify>
  <done>All tests green; TEST-02 requirement satisfied</done>
</task>

</tasks>

<verification>
Verify TEST-02 requirement closure:

1. **Publish-while-offline tested:**
   - handle() gracefully returns when _client is None
   - handle_line_state_change() gracefully returns when _client is None
   - No exceptions, no logging, no deadlock

2. **Connection drop / recovery scenario:**
   - Events dropped when offline (no queue, no retry)
   - Events published when reconnected
   - Behavior confirms broker availability directly affects publishing

3. **All 201+ existing tests remain green** — verified by final pytest run
</verification>

<success_criteria>
- TEST-02 requirement in REQUIREMENTS.md marked complete
- 6+ new tests in test_mqtt_output.py, all passing
- New TestMqttPublishOffline class with clear offline scenarios
- Zero regressions (all 201+ tests still green)
- Code paths in src/adapters/mqtt.py (handle, handle_line_state_change) verified for offline behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-validation/04-02-SUMMARY.md` with:
- What was implemented (TestMqttPublishOffline class, 6+ tests)
- Offline scenarios covered (event drop, line state change drop, recovery)
- Files modified and new lines
- Design decisions (drop vs queue, silent vs logged)
</output>
